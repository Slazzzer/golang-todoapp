HTTP_ADDR=:5050
HTTP_SHUTDOWN_TIMEOUT=30s
HTTP_READ_HEADER_TIMEOUT=10s
HTTP_READ_TIMEOUT=15s
HTTP_WRITE_TIMEOUT=15s
HTTP_IDLE_TIMEOUT=60s
HTTP_MAX_BODY_BYTES=1048576

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_HOST_PORT=5433
POSTGRES_TIMEOUT=10s
POSTGRES_SSLMODE=disable

LOGGER_LEVEL=DEBUG

TIME_ZONE=UTC

# Публикация todoapp: 127.0.0.1 — только локально; 0.0.0.0 — доступ снаружи (сервер)
TODOAPP_PORT_BINDING=127.0.0.1:5050:5050

# CORS: origins через запятую — полный URL (http://host:port), hostname или null (file://)
ALLOWED_ORIGINS=http://localhost:5050,http://127.0.0.1:5050,null,80.249.145.151,http://80.249.145.151:5050